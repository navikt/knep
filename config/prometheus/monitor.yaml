
# Prometheus Monitor Service (Metrics)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    control-plane: knep
    app.kubernetes.io/name: servicemonitor
    app.kubernetes.io/instance: knep-metrics-monitor
    app.kubernetes.io/component: metrics
    app.kubernetes.io/created-by: knep
    app.kubernetes.io/part-of: knep
    app.kubernetes.io/managed-by: kustomize
  name: knep-metrics-monitor
  namespace: system
spec:
  endpoints:
    - path: /metrics
      port: https
      scheme: https
      bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      tlsConfig:
        insecureSkipVerify: true
  selector:
    matchLabels:
      control-plane: knep
